package com.example.account.modules.paymentgateway.dto.enums;

/**
 * Mirrors Kernel's InitiatePaymentRequest.method enum exactly
 * (kernel-core.yowyob.com /api/payments/orders).
 */
public enum PaymentMethodType {
    MOBILE_MONEY,
    CARD
}
